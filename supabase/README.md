# Historical Supabase SQL (reference only)

The application **no longer uses Supabase** at runtime.

Stack now:

- MongoDB + Mongoose
- NextAuth (JWT)
- Cloudinary

Files in this folder are kept only as migration history from an earlier prototype.
Do not run these migrations against production for the current app.
