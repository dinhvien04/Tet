# Legacy Supabase-era tests

These tests were written against the old Supabase backend and are **not** part of CI.

Current stack: **MongoDB + NextAuth**. Use `npm run test:ci` for maintained tests under `tests/`.

Do not import `@supabase/*` in new tests.
